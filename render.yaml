services:
  - type: web
    name: njt-departure-prediction
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /healthz

